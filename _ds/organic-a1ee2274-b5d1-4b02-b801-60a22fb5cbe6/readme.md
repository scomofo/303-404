# Studio Rack design system

Dark control-room ground, walnut side cheeks, 808-orange LEDs, phosphor green, VT323 LCD on live step grids, cream printed Song/Drum charts via `.chart-sheet`.

Tokens live in `styles.css`. Classes: `.lcd`, `.chart-sheet`, plus the existing component set.
